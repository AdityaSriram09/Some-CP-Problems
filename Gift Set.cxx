#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
int ans(){
	ll x,y,a,b;
	cin>>x>>y>>a>>b;
	ll miin = min(a,b),maax=max(a,b),count = 0,k;
	if(a==b) return miin/
	for(int i=0;i<=n;i++){
		
		}
	}

int main(){
	int t;
	cin>>t;
	while(t--)
	cout<<ans()<<endl;
	return 0;
	}
	
